// função de soma onde é preciso fornecer os dois parâmetros. caso não seja fornecido, considera 0 (=0)
function soma(n1=0, n2=0) {
    return n1 + n2
}

console.log(soma(2, 8))     // fornece os parâmetros e imprime no console